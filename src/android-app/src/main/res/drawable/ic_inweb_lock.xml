<?xml version="1.0" encoding="utf-8"?>
<!-- iNWEB Browser — Locked icon.
     Hand-authored geometry (no third-party icon pack, so no separate
     licence or attribution). Stroke colour is black by design: Compose's
     Icon() tints the painter with LocalContentColor, so these follow the
     Material 3 content colour in light and dark themes.
     Replaces androidx.compose.material.icons, which is absent from the
     pinned Chromium tree (docs/HOME-PAGE-HANDOFF.md A10). -->
<vector xmlns:android="http://schemas.android.com/apk/res/android"
    android:width="24dp"
    android:height="24dp"
    android:viewportWidth="24"
    android:viewportHeight="24">
    <path
        android:pathData="M7,11V8a5,5 0 0 1 10,0v3"
        android:strokeColor="#FF000000"
        android:strokeWidth="2"
        android:strokeLineCap="round"
        android:strokeLineJoin="round" />
    <path
        android:pathData="M5,11h14v10H5z"
        android:strokeColor="#FF000000"
        android:strokeWidth="2"
        android:strokeLineCap="round"
        android:strokeLineJoin="round" />
</vector>

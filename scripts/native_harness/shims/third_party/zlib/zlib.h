// Host-harness shim: maps Chromium's third_party/zlib include path to
// the system zlib (the real tree builds against the bundled copy).
#ifndef INWEB_HARNESS_ZLIB_SHIM_
#define INWEB_HARNESS_ZLIB_SHIM_
#include <zlib.h>
#endif
